apiVersion: v1
kind: Service
metadata:
  name: graphql
  namespace: transcode
  labels:
    app: graphql
spec:
  type: LoadBalancer  # Exposes GraphQL externally
  ports:
    - port: 8081
      targetPort: 8081
      name: http
  selector:
    app: graphql
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphql
  namespace: transcode
  labels:
    app: graphql
spec:
  replicas: 2
  selector:
    matchLabels:
      app: graphql
  template:
    metadata:
      labels:
        app: graphql
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: graphql
          image: transcode-graphql:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: transcode-config
                  key: GRAPHQL_PORT
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: transcode-secrets
                  key: DATABASE_URL
            - name: REDIS_ADDR
              valueFrom:
                configMapKeyRef:
                  name: transcode-config
                  key: REDIS_ADDR
            - name: S3_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: transcode-config
                  key: S3_ENDPOINT
            - name: S3_PUBLIC_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: transcode-config
                  key: S3_PUBLIC_ENDPOINT
            - name: S3_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: transcode-secrets
                  key: S3_ACCESS_KEY
            - name: S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: transcode-secrets
                  key: S3_SECRET_KEY
            - name: S3_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: transcode-config
                  key: S3_BUCKET
            - name: S3_REGION
              valueFrom:
                configMapKeyRef:
                  name: transcode-config
                  key: S3_REGION
            - name: S3_USE_PATH_STYLE
              valueFrom:
                configMapKeyRef:
                  name: transcode-config
                  key: S3_USE_PATH_STYLE
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
